# ADR-NNNN — <short imperative title>

**Date:** YYYY-MM-DD · **Status:** accepted · **Task:** <board id, if any>

## Context

What forces a decision now — the constraint, the tension, the thing that breaks if we don't choose. Cite the ADRs this builds on. Keep it to what a reader needs to judge the decision.

## Decision

The choice, stated so it can be implemented without re-deriving it. Number the parts. Name the files/functions where it lives. If it touches the engine or seed, say how the eval harness (ADR-0006) and seed-check stay green.

## Proof-only guardrails

(If the decision touches customer-facing copy, metrics, or demo data.) How it stays inside ADR-0002: no fabricated proof, confidence bands not hard dates, measured-not-projected, demo watermarked.

## Consequences

What this makes easier or harder later; anything now load-bearing on it.

## Alternatives rejected

The options considered and why they lost — the most useful part for a future reader who wonders "why not X".

## Kill-criteria

(Optional.) The observation that would tell us this decision was wrong, so we revisit deliberately rather than drift.
